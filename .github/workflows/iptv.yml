name: Update MIGU Live Channels

on:
  schedule:
    # 每2小时运行一次（UTC时间）
    - cron: '0 */2 * * *'
  workflow_dispatch:  # 支持手动触发
  push:  # 代码变更时也运行
    paths:
      - '**.py'
    branches: [ main, master ]

jobs:
  update-channels:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        # 保留上次生成的文件
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Run MIGU channel updater
      run: |
        python migu.py  # 假设你的脚本名为migu.py

    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # 检查是否有文件变更
        if git diff --quiet HEAD -- migu.m3u migu.txt; then
          echo "No changes detected in output files"
        else
          git add migu.m3u migu.txt
          git commit -m "🤖 Auto-update: MIGU直播源 $(date +'%Y-%m-%d %H:%M')"
          git push
        fi
      env:
        # 如果有需要，添加你的GitHub Token
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
